---
export interface Props {
	name: string;
}

const { name } = Astro.props;
---

<li class="my-1 mx-3 inline-block xs:my-2 md:my-3">
	<a
		href={`/tags/${name.toLowerCase()}`}
		class="link group text-sm md:text-lg"
	>
		<span>#{name.toLowerCase()}</span>
	</a>
</li>
