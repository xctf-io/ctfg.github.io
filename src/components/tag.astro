---
export interface Props {
	name: string;
}

const { name } = Astro.props;
---

<li
	class="inline-block md:my-3 xs:my-2 my-1 mx-3"
>
	<a
		href={`/tags/${name.toLowerCase()}`}
		class="text-sm md:text-lg group link"
	>
		<span>#{name.toLowerCase()}</span>
	</a>
</li>
